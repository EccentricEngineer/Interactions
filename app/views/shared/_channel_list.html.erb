<ul>
  <% current_user.channelusers.each do |element| %>
    <div class="channel-avatar">
      <%= cl_image_tag element.channel.photo.key, height: 50, crop: :fill %>
      <div class="channel-text">
        <h4 class="channel-title">@<%= element.channel.name %></h4>
      </div>
    </div>
  <% end %>
</ul>
